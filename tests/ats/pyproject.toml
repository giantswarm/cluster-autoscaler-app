[project]
name = "cluster-autoscaler-app-tests"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "pytest-helm-charts~=1.3.2",
]

[tool.pytest.ini_options]
markers = [
    "smoke",
    "functional",
    "upgrade",
]
